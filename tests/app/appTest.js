'use strict';

var j = require(jasmine);

j.describe('Testy filtrow uzywanych w aplikacji', function () {
    j.it('powinno nic sie nie dziac', function () {
        expect(true).toBeTruthy();
    });
});
